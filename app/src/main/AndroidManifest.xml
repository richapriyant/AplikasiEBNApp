<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.destiny.aplikasiebn">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.SejarahActivity"></activity>
        <activity android:name=".Activity.ListCaraBatikActivity" />
        <activity android:name=".Activity.TentangActivity" />
        <activity android:name=".Activity.FavoritActivity" />
        <activity android:name=".Activity.CaraBatikActivity" />
        <activity android:name=".Activity.DetailActivity" />
        <activity android:name=".Activity.BatikActivity" />
        <activity android:name=".Activity.HomeActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>